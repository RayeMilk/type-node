// Return Annotations ( Regular )

// กำหนดค่าส่งออกมา ( type )

function double1(x:number):number {
    return x * x;
}

const resd1 = double1(10);
console.log(resd1);

//Arrow Function

const doublec2 = (x:number):number => x * x;

const resd2 = doublec2(10);
console.log(resd2);
